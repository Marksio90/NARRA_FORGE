# ╔══════════════════════════════════════════════════════════════════════════╗
# ║                   NARRA_FORGE V2 - Environment Variables                 ║
# ║                                                                          ║
# ║  SETUP INSTRUCTIONS:                                                     ║
# ║  1. Copy this file:  cp .env.example .env                               ║
# ║  2. Generate secrets (see SECURITY section below)                        ║
# ║  3. Set your API keys (at minimum OPENAI_API_KEY)                       ║
# ║  4. Adjust settings for your environment (dev/staging/production)        ║
# ║                                                                          ║
# ║  ⚠️  IMPORTANT: Never commit .env to git! It's in .gitignore            ║
# ╚══════════════════════════════════════════════════════════════════════════╝

# ══════════════════════════════════════════════════════════════════════════
# 🔐 SECURITY - CRITICAL SETTINGS (REQUIRED)
# ══════════════════════════════════════════════════════════════════════════
#
# Generate secure values with:
#   python -c "import secrets; print(secrets.token_hex(32))"
#
# Or use our built-in generator:
#   python scripts/generate_secrets.py
#
# NEVER use the example values in production!

# JWT Secret Key - REQUIRED, minimum 32 characters
# This is validated on startup and will fail if weak
JWT_SECRET_KEY=CHANGE-THIS-use-python-secrets-token-hex-32-minimum-32-chars
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# Redis Password - REQUIRED for production
# In development, you can leave empty for local Redis without auth
REDIS_PASSWORD=CHANGE-THIS-redis-password-use-secrets-token-hex-32

# Database Password - REQUIRED
# Must match the password in docker-compose.yml for PostgreSQL
DB_PASSWORD=CHANGE-THIS-database-password-use-secrets-token-hex-32


# ══════════════════════════════════════════════════════════════════════════
# 🗄️  DATABASE CONFIGURATION
# ══════════════════════════════════════════════════════════════════════════

# PostgreSQL Database Settings
DB_USER=narra_forge
DB_HOST=postgres
DB_PORT=5432
DB_NAME=narra_forge

# Optional: Override auto-constructed DATABASE_URL
# Format: postgresql+asyncpg://user:password@host:port/database
# DATABASE_URL=postgresql+asyncpg://narra_forge:yourpassword@postgres:5432/narra_forge

# Database Connection Pool Settings
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=10
DB_POOL_PRE_PING=true
DB_ECHO=false


# ══════════════════════════════════════════════════════════════════════════
# 🔴 REDIS & CELERY CONFIGURATION
# ══════════════════════════════════════════════════════════════════════════

# Redis Connection
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# Auto-constructed Redis URLs (use password from above)
# Format: redis://:password@host:port/db
REDIS_URL=redis://:CHANGE-THIS-redis-password-use-secrets-token-hex-32@redis:6379/0

# Celery Task Queue
CELERY_BROKER_URL=redis://:CHANGE-THIS-redis-password-use-secrets-token-hex-32@redis:6379/0
CELERY_RESULT_BACKEND=redis://:CHANGE-THIS-redis-password-use-secrets-token-hex-32@redis:6379/1
CELERY_TASK_TRACK_STARTED=true
CELERY_TASK_TIME_LIMIT=7200

# Redis Cache Settings
REDIS_CACHE_TTL=3600


# ══════════════════════════════════════════════════════════════════════════
# 🤖 AI API KEYS (REQUIRED - At least one)
# ══════════════════════════════════════════════════════════════════════════
#
# Get your keys from:
#   OpenAI:    https://platform.openai.com/api-keys
#   Anthropic: https://console.anthropic.com/settings/keys
#
# IMPORTANT: At least ONE API key is required for the platform to work

# OpenAI API Key (Primary - REQUIRED for narrative generation)
OPENAI_API_KEY=sk-proj-PASTE-YOUR-OPENAI-API-KEY-HERE

# Anthropic API Key (Optional - for Claude models)
ANTHROPIC_API_KEY=


# ══════════════════════════════════════════════════════════════════════════
# 🌐 API & APPLICATION SETTINGS
# ══════════════════════════════════════════════════════════════════════════

# Application Info
APP_NAME=NARRA_FORGE
APP_VERSION=2.0.1
APP_DESCRIPTION=Professional AI-powered narrative generation platform

# Server Settings
HOST=0.0.0.0
PORT=8000
DEBUG=false
RELOAD=false

# Environment (development, staging, production)
ENVIRONMENT=development


# ══════════════════════════════════════════════════════════════════════════
# 🔒 CORS & SECURITY
# ══════════════════════════════════════════════════════════════════════════
#
# DEVELOPMENT: Use localhost
# PRODUCTION: Use your actual domains (comma-separated, no spaces)

# Allowed Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:8080

CORS_ALLOW_CREDENTIALS=true
CORS_ALLOW_METHODS=GET,POST,PUT,DELETE,PATCH,OPTIONS
CORS_ALLOW_HEADERS=*


# ══════════════════════════════════════════════════════════════════════════
# 🚦 RATE LIMITING
# ══════════════════════════════════════════════════════════════════════════

RATE_LIMIT_ENABLED=true
RATE_LIMIT_PER_MINUTE=60
RATE_LIMIT_BURST=100


# ══════════════════════════════════════════════════════════════════════════
# 📊 MONITORING & OBSERVABILITY
# ══════════════════════════════════════════════════════════════════════════

# Prometheus Metrics
ENABLE_METRICS=true
METRICS_PORT=9090

# Sentry Error Tracking (Optional but recommended for production)
ENABLE_SENTRY=false
SENTRY_DSN=
SENTRY_ENVIRONMENT=development
SENTRY_TRACES_SAMPLE_RATE=0.1

# Logging Level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO


# ══════════════════════════════════════════════════════════════════════════
# 🎨 FRONTEND CONFIGURATION
# ══════════════════════════════════════════════════════════════════════════

# API URL for Next.js frontend
# DEVELOPMENT: http://localhost:8000
# PRODUCTION:  https://api.yourdomain.com
NEXT_PUBLIC_API_URL=http://localhost:8000


# ══════════════════════════════════════════════════════════════════════════
# 🔐 OAUTH2 PROVIDERS (Optional)
# ══════════════════════════════════════════════════════════════════════════

# Google OAuth2
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=

# GitHub OAuth2
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GITHUB_REDIRECT_URI=


# ══════════════════════════════════════════════════════════════════════════
# 📁 FILE UPLOAD
# ══════════════════════════════════════════════════════════════════════════

MAX_UPLOAD_SIZE_MB=10
ALLOWED_UPLOAD_TYPES=txt,pdf,docx


# ══════════════════════════════════════════════════════════════════════════
# 📄 PAGINATION
# ══════════════════════════════════════════════════════════════════════════

DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100


# ══════════════════════════════════════════════════════════════════════════
# 📚 API DOCUMENTATION
# ══════════════════════════════════════════════════════════════════════════

DOCS_URL=/docs
REDOC_URL=/redoc
OPENAPI_URL=/openapi.json


# ══════════════════════════════════════════════════════════════════════════
# 🌐 WEBSOCKET SETTINGS
# ══════════════════════════════════════════════════════════════════════════

WS_HEARTBEAT_INTERVAL=30
WS_MAX_CONNECTIONS=1000


# ══════════════════════════════════════════════════════════════════════════
# 📊 GRAFANA CONFIGURATION
# ══════════════════════════════════════════════════════════════════════════

GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=CHANGE-THIS-grafana-password


# ══════════════════════════════════════════════════════════════════════════
# 🐳 DOCKER BUILD CONFIGURATION
# ══════════════════════════════════════════════════════════════════════════

# BuildKit for faster, better docker builds
BUILDKIT_PROGRESS=plain
COMPOSE_DOCKER_CLI_BUILD=1
DOCKER_BUILDKIT=1


# ══════════════════════════════════════════════════════════════════════════
# 📧 EMAIL CONFIGURATION (Optional - for notifications)
# ══════════════════════════════════════════════════════════════════════════

SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@yourdomain.com
SMTP_USE_TLS=true


# ══════════════════════════════════════════════════════════════════════════
# 📝 NOTES & QUICK START GUIDE
# ══════════════════════════════════════════════════════════════════════════
#
# MINIMAL SETUP (Development):
# 1. Generate secrets:
#      python -c "import secrets; print('JWT:', secrets.token_hex(32))"
#      python -c "import secrets; print('DB:', secrets.token_hex(32))"
#      python -c "import secrets; print('Redis:', secrets.token_hex(32))"
#
# 2. Set the generated secrets above in JWT_SECRET_KEY, DB_PASSWORD, REDIS_PASSWORD
#
# 3. Get OpenAI API key from: https://platform.openai.com/api-keys
#    Set it in OPENAI_API_KEY
#
# 4. Build and run:
#      docker-compose build
#      docker-compose up -d
#
# 5. Check health:
#      curl http://localhost:8000/health
#      curl http://localhost:8000/health/ready
#
# 6. Access:
#      API Docs:  http://localhost:8000/docs
#      Frontend:  http://localhost:3000
#      Grafana:   http://localhost:3001
#
# PRODUCTION CHECKLIST:
# ✅ Change ALL default passwords and secrets
# ✅ Set ENVIRONMENT=production
# ✅ Set DEBUG=false
# ✅ Update CORS_ORIGINS to your domain
# ✅ Enable Sentry (ENABLE_SENTRY=true) with your DSN
# ✅ Use strong, unique passwords (min 32 chars)
# ✅ Set up SSL/TLS certificates
# ✅ Review and adjust rate limits
# ✅ Configure email for notifications
# ✅ Set up automated backups
# ✅ Never expose .env file
#
# ENVIRONMENT TEMPLATES:
#
# Development:
#   DEBUG=true
#   ENVIRONMENT=development
#   CORS_ORIGINS=http://localhost:3000,http://localhost:8080
#   NEXT_PUBLIC_API_URL=http://localhost:8000
#
# Production:
#   DEBUG=false
#   ENVIRONMENT=production
#   CORS_ORIGINS=https://app.yourdomain.com,https://www.yourdomain.com
#   NEXT_PUBLIC_API_URL=https://api.yourdomain.com
#   ENABLE_SENTRY=true
#   SENTRY_DSN=https://your-sentry-dsn
#
# ══════════════════════════════════════════════════════════════════════════
