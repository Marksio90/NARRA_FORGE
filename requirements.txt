# NARRA_FORGE Dependencies
# Python 3.11+

# OpenAI API (ONLY provider)
openai>=1.54.0
httpx>=0.27.0  # Required for OpenAI client

# Data & Storage
pydantic==2.9.2
pydantic-settings==2.6.0
email-validator==2.1.0  # Required for Pydantic EmailStr validation

# Database
sqlalchemy==2.0.35
aiosqlite==0.20.0
psycopg2-binary==2.9.9  # PostgreSQL adapter (moved from duplicate section)
asyncpg==0.29.0  # Async PostgreSQL adapter (moved from duplicate section)

# Utilities
python-dotenv==1.0.1
tenacity==9.0.0  # Retry logic
tiktoken==0.8.0  # Token counting

# CLI & UI
click==8.1.7
rich==13.9.4  # Beautiful CLI output
tqdm==4.67.0  # Progress bars

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==6.0.0

# Development
black==24.10.0
ruff==0.7.4
mypy==1.13.0

# Monitoring & Observability
structlog==24.4.0  # Structured logging
prometheus-client==0.21.0  # Prometheus metrics
sentry-sdk==1.40.0  # Error tracking & performance monitoring

# API & Backend (Phase 2)
fastapi==0.109.0  # Web framework
uvicorn[standard]==0.27.0  # ASGI server
alembic==1.13.1  # Database migrations
anthropic==0.39.0  # Anthropic API for Claude models

# Authentication & Security
python-jose[cryptography]==3.3.0  # JWT tokens
passlib[bcrypt]==1.7.4  # Password hashing
python-multipart==0.0.6  # Form data parsing

# Task Queue
celery==5.3.6  # Distributed task queue
redis==5.0.1  # Redis client
flower==2.0.1  # Celery monitoring

# WebSockets
websockets==12.0  # WebSocket support
